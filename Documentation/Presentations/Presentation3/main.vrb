\frametitle{Example: Using Enums in C++}
\begin{lstlisting}[language=C++]
// Avoiding magic numbers using enum
enum Direction {
  North = 0,
  East = 1,
  South = 2,
  West = 3
};

void move(Direction dir) {
  if (dir == North) {
      // Move north
  } else if (dir == East) {
      // Move east
  }
  // and so on...
}
  \end{lstlisting}
